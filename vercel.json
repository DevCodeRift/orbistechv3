{
  "framework": "nextjs",
  "buildCommand": "npm install --prefix apps/web-app && cd apps/web-app && npm run build",
  "outputDirectory": "apps/web-app/.next",
  "installCommand": "npm install",
  "functions": {
    "apps/web-app/app/api/**/*.{js,ts}": {
      "maxDuration": 30
    }
  }
}