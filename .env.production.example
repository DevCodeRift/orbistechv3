# Production Environment Variables for Tenant App
# Deploy these in Vercel dashboard

# Database - Use Railway PostgreSQL or similar
DATABASE_URL="postgresql://username:password@host:port/database"

# NextAuth Configuration - Will be set dynamically per subdomain
NEXTAUTH_SECRET="generate-new-32-byte-secret-for-production"
NEXTAUTH_URL="https://orbistech.dev" # Vercel will handle subdomain routing

# Discord OAuth Configuration
DISCORD_CLIENT_ID="your-discord-application-id"
DISCORD_CLIENT_SECRET="your-discord-client-secret"

# Encryption Key - Generate new one for production (same as admin)
ENCRYPTION_KEY="generate-new-32-byte-encryption-key"

# Environment
NODE_ENV="production"